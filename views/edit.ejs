<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <form action="/update/<%= user._id%>" method="post">
            <div class="nav mb-10">
                <a href="/read" class="text-blue-500">Update Users</a>
            </div>
            <h1 class="text-3xl tracking-tighter mb-3">Create User</h1>
            <input type="text"  name= "name" placeholder="Name" class="px-5 py-2 bg-transparent border-2 border-zinc-800 rounded-lg" value="<%= user.name%>">
            <input type="text"  name= "email" placeholder="Email" class="px-5 py-2 bg-transparent border-2 border-zinc-800 rounded-lg" value="<%= user.email%>">
            <input type="text"  name= "imageurl" placeholder="Image URL" class="px-5 py-2 bg-transparent border-2 border-zinc-800 rounded-lg" value="<%= user.imageurl%>">
            <input type="submit" value="Update User"  class="px-5 py-2 bg-yellow-500 text-black rounded-lg ml-3">
        </form>
    </div>
</body>
</html>